const button = document.getElementsByTagName('convert-button')
const dolar = 5.2

const convertValues = () => {
    const input = document.getElementById('input-real').value
    
}

button.addEventListener('click', convertValues)